# CLAUDE.md

이 저장소는 **업무 에이전트 모음**이다. 코드는 없고 전부 마크다운이며, 실행 주체는 루트 `.claude/`의 서브에이전트와 스킬이다.

## 라우팅 — 어느 폴더의 일인가

| 폴더 | 에이전트 | 무엇을 하나 | 진입점 |
|---|---|---|---|
| [sgt-planning/](sgt-planning/) | SGT 기획 에이전트 4종 | 시장조사 → 기획 종합 → 비관 검토 → 이슈 초안 | `/plan-cycle` |
| [sgt-release/](sgt-release/) | SGT 배포 패키징 에이전트 | (기획 중) | — |

작업 요청이 어느 폴더에 속하는지 먼저 판단하고, **그 폴더의 `CLAUDE.md`를 읽은 뒤** 시작한다. 도메인 규칙(SGT 저장소 참조 규약 등)은 전부 거기 있다.

## 저장소 공통 규칙

- **에이전트·스킬 정의는 루트 `.claude/`에만 둔다.** 하위 폴더에 `.claude/`를 만들지 마라 — 서브에이전트가 로드되지 않거나 어디서 왔는지 추적이 안 된다. 이름이 겹칠 것 같으면 파일명·`name:`에 에이전트 접두어를 붙인다(`sgt-plan-*`, `sgt-rel-*`)
- **경로는 저장소 루트 기준.** 에이전트·스킬 정의에 경로를 쓸 때는 항상 폴더 접두어를 붙인다 (`sgt-planning/cycles/...`)
- **산출물은 자기 폴더 안에만 쓴다.** 다른 에이전트 폴더를 건드리지 마라
- **파일 존재 자체가 상태다.** 진행 상황을 별도로 기록하지 말고, 루트에 통합 상태 보드를 만들지 마라 — 상태는 각 폴더가 갖는다
- 문서를 쓸 때는 [writing-style.md](writing-style.md)를 따른다 (`humanize-korean` 스킬 + 보존·제외 규칙). 이 파일만 에이전트 공용이라 루트에 있다

## 새 에이전트 추가하기

1. `<slug>/` 폴더 + `<slug>/CLAUDE.md`(도메인 규칙) + `<slug>/README.md`(상태 보드)
2. 필요하면 `.claude/agents/<slug>-*.md`, `.claude/skills/<이름>.skill/`
3. 위 라우팅 표와 [README.md](README.md)에 한 줄 추가
